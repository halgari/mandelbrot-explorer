[project]
name = "mandelbrot-explorer"
version = "0.1.0"
description = "GPU-accelerated Mandelbrot set explorer"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "moderngl>=5.12.0",
    "mpmath>=1.4.1",
    "pygame-ce>=2.5.7",
    "pyinstaller>=6.19.0",
]
